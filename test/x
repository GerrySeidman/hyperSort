Save Test

var people = peopleMaker.makePeople(40);

for (var i=people.length/2; i < people.length; i++) {
	people[i].last= 'Seidman'
}

	

	// var sort = quicksort_by(function(d) {
	// 	return d.last;
	// });

	// console.log("Sort: " + sort);

	
	// sort(people, 0, people.length);

	quicksort(people, 'last');

	for(var i=0; i < people.length; i++) {
		console.log(people[i].toString());
	}
